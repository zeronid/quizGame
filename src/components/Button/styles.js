import { Colors } from '../../theme'

const styles = {
    button: {
        minWidth: '200px',
        height: '50px',
        fontWeight: 'bold',
        borderRadius: '20px',
        color: Colors.main,
        backgroundColor: Colors.text,
        borderColor: Colors.main,
    }
}

export default styles