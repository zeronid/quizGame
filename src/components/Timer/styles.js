import { Colors } from '../../theme'

const styles = {
    timer: {
        position: 'absolute',
        top: 50,
        right: 50,
        fontSize: '100px ',
        color: Colors.blue
    }
}

export default styles